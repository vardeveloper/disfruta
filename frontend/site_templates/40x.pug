doctype
html
  head
    meta(charset='utf-8')
    meta(http-equiv='X-UA-Compatible' content='IE=edge')
    meta(name='viewport' content='width=device-width,initial-scale=1.0')
    title Disfruta Profuturo
    style.
      html, body {
        margin: 0;
        padding: 0;
        font-family: Arial,Helvetica Neue,Helvetica,sans-serif;
        -webkit-font-smoothing: antialiased;
        color: #757575;
      }

      .btn {
        background-color: #EE3224;
        display: flex;
        width: 100%;
        max-width: 264px;
        height: 50px;
        margin-bottom: 20px;
        color: #fff;
        border-radius: 5px;
        border: none;
        cursor: pointer;
        font-size: 16px;
        text-decoration: none;
        justify-content: center;
        align-items: center;
      }

      h1 {
        color: #EE3224;
        font-size: 30px;
        font-weight: bold;
        margin: 0 0 20px;
      }

      p {
        font-size: 16px;
        margin: 0 0 20px;
      }

      #header {
        width: 95.5%;
        height: 72px;
        margin: 0 auto;
        display: flex;
        align-items: center;
        justify-content: space-between;
        box-sizing: border-box;
      }

      #disfruta-logo {
        width: 111px;
        height: 42px;
      }

      #profuturo-logo {
        width: 135px;
        height: 47px;
      }

      #message-type {
        margin: 0 auto 20px;
        max-width: 399px;
      }

      #main {
        width: 90%;
        max-width: 589px;
        margin: 0 auto;
        text-align: center;
        padding-bottom: 20px;
      }

      #main .btn {
        width: 280px;
        margin: 0 auto;
      }

      #wrap {
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        min-width: 320px;
        display: flex;
        align-items: center;
        justify-content: center;
      }

      @media only screen and (max-height: 600px) {
        #wrap {
          position: static;
          display: block;
        }
      }

  body
    #header
      #disfruta-logo
        include ../public/svg/logo-disfruta.svg
      #profuturo-logo
        include ../public/svg/logo-profuturo.svg

    #wrap
      #main
        #message-type
          block picture
            include ../public/svg/error-404.svg
        #message-content
          block message
            h1 &iexcl;Ups!
            p No hemos podido encontrar la p&aacute;gina que buscas o no tienes permiso para verla.
            a.btn( href='/' ) Volver al inicio
